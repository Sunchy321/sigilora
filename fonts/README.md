# fonts

Canonical source data directory for each game.

Each game has its own subdirectory (`fonts/<game>/`) containing:

- Normalized SVGs
- Ligature text mappings
- Glyph identifiers and display information
- Font internal version
- COLR target version
- Asset source and license records

These are the project's long-term source data; build tool configuration is not a public contract.
