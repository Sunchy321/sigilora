---
title: 桌面安装
description: 在桌面系统安装 Sigilora 字体。
---

# 桌面安装

桌面字体以 TTF 形式随每个 GitHub Release 发布。

1. 下载 Release 归档 `Sigilora-Fonts-<version>.zip`。
2. 解压并安装 `.ttf` 文件（macOS 或 Windows 上双击即可）。
3. 在应用中选用该字体，例如 **Sigilora Magic** 或 **Sigilora Lorcana**。

对于 COLRv1 字体族——宝可梦卡牌与符文战场的全量字体，以及符文战场 Lite——归档中还包含一份 `… V0` TTF：同一字体以 COLRv0 编码。常规使用安装标准字体族即可；仅当应用在不支持 COLRv1 的上下文（如内嵌 WebKit webview）中渲染文本时，才需要安装 `… V0` 字体族。宝可梦卡牌 Lite 本身已是 COLRv0 字体，因此没有 `V0` 变体。

见 [安装](/docs/guide/install) 了解 Web 用法。
