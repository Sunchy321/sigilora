---
title: npm 包
description: 使用 @sigilora/fonts npm 包。
---

# npm 包

所有游戏字体通过单一 npm 包 `@sigilora/fonts` 发布。

```bash
npm install @sigilora/fonts
```

包内按游戏提供独立 CSS 与 WOFF2 入口。只导入需要的游戏入口，浏览器也只请求实际引用的 WOFF2。

见 [安装](/docs/guide/install) 了解 CSS 用法。
